# Time Capsule

A project to encapsulate moments in time and preserve them for future generations. 

## Project Structure
- `frontend/`: Contains all the frontend code.
- `backend/`: Contains all the backend code.